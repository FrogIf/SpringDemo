server:
  port: 0 # 指示随机使用一个端口
feign:
  client:
    config:
      default:
        connection-timeout: 500
        read-timeout: 500

logging:
  level:
    sch.frog.learn.spring.client.integration.CoffeeService : debug
spring:
  cloud:
    zookeeper:
      connect-string: 192.168.99.100:2181 # zookeeper所在地址
